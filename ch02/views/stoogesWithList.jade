extends layout

block content
  if (stooge && stooge != 'list')
    p #{stooge} is my favourite stooge.
  else if(stooge == 'list')
    p Possible values are: larry, curly, and moe.
  else
    p no stooges listed